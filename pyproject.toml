[project]
name = "loan-risk-evaluator"
version = "1.0.0"
description = "Loan risk evaluator Flask API"
requires-python = ">=3.12"
dependencies = [
    "flask"
]

[project.scripts]
app = "app.api:app"
